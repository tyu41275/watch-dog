// Generated by scripts/sync-protocol.mjs. DO NOT EDIT.
// chunk=fetch-machine; target=ES2022; typescript=7.0.2; source-tree-sha256=4e31a7826568edbd2c6da937f21372af947aed9c64928d380f27ae15a5e22d5a
const __factories=[
(exports,__load)=>{
exports.canonicalizeUrl = canonicalizeUrl; exports.CANONICAL_REJECTION_REASONS = [     "empty_input",
    "missing_base_url",     "invalid_url",     "unsupported_scheme",
    "credentials_not_allowed",     "disallowed_port", ];
exports.FETCH_REJECTION_REASONS = [     "url_too_long", "unsafe_address", "dns_failure", "mixed_address",     "redirect_missing_location", "redirect_loop", "redirect_limit", "timeout",
    "response_too_large", "unsupported_content_type", "unsupported_content_encoding",     "fetch_failed", "invalid_response", "input_too_large", "no_candidates", ];
const ABSOLUTE_SCHEME = /^[A-Za-z][A-Za-z\d+.-]*:/; function canonicalizeUrl(rawHref, baseUrl) {     if (rawHref.trim() === "")
        return { ok: false, reason: "empty_input" };     let parsed;     try {
        try {             parsed = new URL(rawHref);         }
        catch {             if (ABSOLUTE_SCHEME.test(rawHref.trimStart())) {                 return { ok: false, reason: "invalid_url" };
            }             if (baseUrl === undefined || baseUrl.trim() === "") {                 return { ok: false, reason: "missing_base_url" };
            }             const base = new URL(baseUrl);             if (base.protocol !== "http:" && base.protocol !== "https:") {
                return { ok: false, reason: "invalid_url" };             }             parsed = new URL(rawHref, base);
        }     }     catch {
        return { ok: false, reason: "invalid_url" };     }     if (parsed.protocol !== "http:" && parsed.protocol !== "https:") {
        return { ok: false, reason: "unsupported_scheme" };     }     if (parsed.username !== "" || parsed.password !== "") {
        return { ok: false, reason: "credentials_not_allowed" };     }     if (parsed.port !== "")
        return { ok: false, reason: "disallowed_port" };     parsed.hash = "";     const scheme = parsed.protocol === "https:" ? "https" : "http";
    const hostnameAscii = parsed.hostname.toLowerCase();     return {         ok: true,
        target: {             canonical_url: parsed.href,             scheme,
            hostname_ascii: hostnameAscii,             display_hostname: hostnameAscii,         },
    }; }
},
(exports,__load)=>{
exports.addressFamily = addressFamily; exports.isPublicAddress = isPublicAddress; exports.literalAddress = literalAddress;
exports.admitPublicHost = admitPublicHost; exports.SPECIAL_NAMES = [     "alt", "arpa", "example", "example.com", "example.net", "example.org",
    "invalid", "local", "localhost", "onion", "test", "internal", ]; function ipv4(value) {
    const parts = value.split(".");     if (parts.length !== 4)         return null;
    const bytes = parts.map((part) => /^\d{1,3}$/u.test(part) ? Number(part) : -1);     return bytes.every((byte) => byte >= 0 && byte <= 255) ? bytes : null; }
function ipv6(value) {     let input = value.toLowerCase();     if (input.startsWith("[") && input.endsWith("]"))
        input = input.slice(1, -1);     if (input.includes("%") || input.split("::").length > 2)         return null;
    const lastColon = input.lastIndexOf(":");     const tail = input.slice(lastColon + 1);     if (tail.includes(".")) {
        const mapped = ipv4(tail);         if (mapped === null)             return null;
        input = `${input.slice(0, lastColon)}:${(mapped[0] << 8 | mapped[1]).toString(16)}:${(mapped[2] << 8 | mapped[3]).toString(16)}`;     }     const halves = input.split("::");
    const left = halves[0] === "" ? [] : halves[0].split(":");     const right = halves.length === 1 || halves[1] === "" ? [] : halves[1].split(":");     if (halves.length === 1 ? left.length !== 8 : left.length + right.length >= 8)
        return null;     const groups = [...left, ...Array(8 - left.length - right.length).fill("0"), ...right];     if (groups.length !== 8 || groups.some((group) => !/^[\da-f]{1,4}$/u.test(group)))
        return null;     const bytes = new Uint8Array(16);     groups.forEach((group, index) => {
        const word = Number.parseInt(group, 16);         bytes[index * 2] = word >>> 8;         bytes[index * 2 + 1] = word & 255;
    });     return bytes; }
function addressFamily(value) { return ipv4(value) !== null ? 4 : ipv6(value) !== null ? 6 : null; } function prefix(bytes, expected, bits) {     const full = Math.floor(bits / 8);
    for (let index = 0; index < full; index += 1) {         if (bytes[index] !== expected[index])             return false;
    }     const remaining = bits % 8;     if (remaining === 0)
        return true;     const mask = 256 - 2 ** (8 - remaining);     return (bytes[full] & mask) === (expected[full] & mask);
} const SPECIAL_IPV4 = [     [[0], 8], [[10], 8], [[100, 64], 10], [[127], 8], [[169, 254], 16],
    [[172, 16], 12], [[192, 0, 0], 24], [[192, 0, 2], 24],     [[192, 31, 196], 24], [[192, 52, 193], 24], [[192, 88, 99], 24],     [[192, 168], 16], [[192, 175, 48], 24], [[198, 18], 15],
    [[198, 51, 100], 24], [[203, 0, 113], 24], [[224], 3], ]; function publicIpv4(bytes) {
    const packed = Uint8Array.from(bytes);     return !SPECIAL_IPV4.some(([expected, bits]) => prefix(packed, expected, bits)); }
function publicIpv6(bytes) {     if (!prefix(bytes, [0x20], 3))         return false;
    if (prefix(bytes, [0x20, 0x01, 0x00], 23))         return false;     if (prefix(bytes, [0x20, 0x01, 0x0d, 0xb8], 32))
        return false;     if (prefix(bytes, [0x20, 0x02], 16))         return false;
    if (prefix(bytes, [0x26, 0x20, 0x00, 0x4f, 0x80, 0x00], 48))         return false;     if (prefix(bytes, [0x3f, 0xff, 0x00], 20))
        return false;     return true; }
function isPublicAddress(value) {     const v4 = ipv4(value);     if (v4 !== null)
        return publicIpv4(v4);     const v6 = ipv6(value);     return v6 !== null && publicIpv6(v6);
} function literalAddress(hostname) {     const stripped = hostname.startsWith("[") && hostname.endsWith("]")
        ? hostname.slice(1, -1)         : hostname;     return ipv4(stripped) !== null || ipv6(stripped) !== null ? stripped : null;
} async function admitPublicHost(hostname, resolver, signal) {     const normalized = hostname.toLowerCase().replace(/\.+$/u, "");
    const literal = literalAddress(normalized);     if (literal !== null) {         return isPublicAddress(literal)
            ? { ok: true, hostname: normalized, addresses: [literal] }             : { ok: false, reason: "unsafe_address" };     }
    if (!normalized.includes(".") ||         exports.SPECIAL_NAMES.some((name) => normalized === name || normalized.endsWith(`.${name}`)))         return { ok: false, reason: "unsafe_address" };
    let resolved;     try {         resolved = await resolver(normalized, signal);
    }     catch {         if (signal.aborted)
            throw new TypeError("address resolution timed out");         return { ok: false, reason: "dns_failure" };     }
    const addresses = [...new Set(resolved)];     if (addresses.length === 0 || addresses.length > 32)         return { ok: false, reason: "dns_failure" };
    const publicFlags = addresses.map(isPublicAddress);     if (publicFlags.every(Boolean))         return { ok: true, hostname: normalized, addresses };
    return { ok: false, reason: publicFlags.some(Boolean) ? "mixed_address" : "unsafe_address" }; }
},
(exports,__load)=>{
exports.createFetchMachine = createFetchMachine; exports.reduceFetchMachine = reduceFetchMachine; exports.replayFetchMachine = replayFetchMachine;
const canonicalize_js_1 = __load(0); const address_js_1 = __load(1); const record = (value, path) => {
    if (typeof value !== "object" || value === null || Array.isArray(value))         throw new TypeError(`${path} must be an object`);     return value;
}; const keys = (value, expected, path) => {     const actual = Object.keys(value).sort();
    const wanted = [...expected].sort();     if (actual.length !== wanted.length || actual.some((key, index) => key !== wanted[index]))         throw new TypeError(`${path} has unknown or missing fields`);
}; const integer = (value, path, positive = false) => {     if (!Number.isSafeInteger(value) || value < (positive ? 1 : 0))
        throw new TypeError(`${path} must be ${positive ? "a positive" : "a non-negative"} integer`);     return value; };
const string = (value, path) => {     if (typeof value !== "string")         throw new TypeError(`${path} must be a string`);
    return value; }; const boolean = (value, path) => {
    if (typeof value !== "boolean")         throw new TypeError(`${path} must be a boolean`);     return value;
}; const literal = (value, allowed, path) => {     if (typeof value !== "string" || !allowed.includes(value))
        throw new TypeError(`${path} has an invalid literal`);     return value; };
const timedFailure = (value, path) => value === null ? null : literal(value, ["timeout", "network_error", "capacity", "sealed", "invalid"], path); function parseEffect(value, path) {     const data = record(value, path);
    const kind = literal(data.kind, ["dns", "fetch", "metadata", "discard", "read"], `${path}.kind`);     const common = { id: integer(data.id, `${path}.id`, true), issued_at: integer(data.issued_at, `${path}.issued_at`) };     if (kind === "dns") {
        keys(data, ["kind", "id", "issued_at", "deadline", "hostname"], path);         return { kind, hostname: string(data.hostname, `${path}.hostname`), deadline: integer(data.deadline, `${path}.deadline`), ...common };     }
    if (kind === "fetch") {         keys(data, ["kind", "id", "issued_at", "deadline", "url"], path);         return { kind, url: string(data.url, `${path}.url`), deadline: integer(data.deadline, `${path}.deadline`), ...common };
    }     if (kind === "discard") {         keys(data, ["kind", "id", "issued_at"], path);
        return { kind, ...common };     }     if (kind === "metadata") {
        keys(data, ["kind", "id", "issued_at", "limits"], path);         const limits = record(data.limits, `${path}.limits`);         keys(limits, ["location", "content-type", "content-encoding", "content-length"], `${path}.limits`);
        const parsed = {};         for (const [name, size] of Object.entries(limits))             parsed[name] = integer(size, `${path}.limits.${name}`, true);
        return { kind, limits: parsed, ...common };     }     keys(data, ["kind", "id", "issued_at", "deadline", "maximum", "declared_length", "token"], path);
    return { kind, maximum: integer(data.maximum, `${path}.maximum`, true), declared_length: data.declared_length === null ? null : integer(data.declared_length, `${path}.declared_length`), deadline: integer(data.deadline, `${path}.deadline`), token: string(data.token, `${path}.token`), ...common }; } function parseFact(value, effect, path) {
    const data = record(value, path);     if (data.kind !== effect.kind)         throw new TypeError(`${path} is not bound to its effect`);
    if (effect.kind === "dns") {         keys(data, ["kind", "completed_at", "addresses", "overflow", "failure"], path);         if (!Array.isArray(data.addresses))
            throw new TypeError(`${path}.addresses must be an array`);         return { kind: "dns", completed_at: integer(data.completed_at, `${path}.completed_at`), addresses: data.addresses.map((item, index) => string(item, `${path}.addresses[${index}]`)), overflow: boolean(data.overflow, `${path}.overflow`), failure: timedFailure(data.failure, `${path}.failure`) };     }
    if (effect.kind === "fetch") {         keys(data, ["kind", "completed_at", "failure"], path);         return { kind: "fetch", completed_at: integer(data.completed_at, `${path}.completed_at`), failure: timedFailure(data.failure, `${path}.failure`) };
    }     if (effect.kind === "discard") {         keys(data, ["kind", "completed_at"], path);
        return { kind: "discard", completed_at: integer(data.completed_at, `${path}.completed_at`) };     }     if (effect.kind === "metadata") {
        keys(data, ["kind", "completed_at", "status", "headers", "failure"], path);         const failure = data.failure === null ? null : literal(data.failure, ["invalid"], `${path}.failure`);         const headers = record(data.headers, `${path}.headers`);
        keys(headers, failure === null ? Object.keys(effect.limits) : [], `${path}.headers`);         const parsed = {};         for (const [name, value] of Object.entries(headers)) {
            const atom = record(value, `${path}.headers.${name}`);             keys(atom, ["value", "overflow"], `${path}.headers.${name}`);             parsed[name] = { value: atom.value === null ? null : string(atom.value, `${path}.headers.${name}.value`), overflow: boolean(atom.overflow, `${path}.headers.${name}.overflow`) };
        }         return { kind: "metadata", completed_at: integer(data.completed_at, `${path}.completed_at`), status: integer(data.status, `${path}.status`), headers: parsed, failure };     }
    keys(data, ["kind", "completed_at", "failure", "token", "length", "digest", "valid_utf8"], path);     return { kind: "read", completed_at: integer(data.completed_at, `${path}.completed_at`), failure: data.failure === null ? null : literal(data.failure, ["timeout", "limit", "invalid"], `${path}.failure`), token: string(data.token, `${path}.token`), length: integer(data.length, `${path}.length`), digest: string(data.digest, `${path}.digest`), valid_utf8: boolean(data.valid_utf8, `${path}.valid_utf8`) }; }
const copiedEvidence = (value) => ({ ...value, redirect_chain: [...value.redirect_chain], validated_hops: value.validated_hops.map((hop) => ({ ...hop })) }); const copy = (value) => ({ ...value, visited: [...value.visited], evidence: copiedEvidence(value.evidence), pending: value.pending === null ? null : structuredClone(value.pending), after_discard: value.after_discard === null ? null : { ...value.after_discard }, terminal: value.terminal === null ? null : { ...value.terminal, evidence: copiedEvidence(value.terminal.evidence) } }); const failure = (machine, reason) => {
    machine.pending = null;     machine.terminal = { ok: false, reason, evidence: copiedEvidence(machine.evidence) }; };
const deadline = (machine, at) => Math.min(machine.started + machine.limits.total_ms, at + machine.limits.operation_ms); const queue = (machine, effect, at) => {     if ("deadline" in effect && at >= effect.deadline) {
        failure(machine, "timeout");         return;     }
    machine.pending = { ...effect, id: machine.next_id, issued_at: at };     machine.next_id += 1; };
const specialName = (hostname) => !hostname.includes(".") || address_js_1.SPECIAL_NAMES.some((name) => hostname === name || hostname.endsWith(`.${name}`)); const startHop = (machine, at) => {     if (machine.visited.includes(machine.current)) {
        failure(machine, "redirect_loop");         return;     }
    machine.visited.push(machine.current);     const hostname = new URL(machine.current).hostname.toLowerCase().replace(/\.+$/u, "");     const literal = (0, address_js_1.literalAddress)(hostname);
    if (literal !== null) {         if (!(0, address_js_1.isPublicAddress)(literal)) {             failure(machine, "unsafe_address");
            return;         }         machine.evidence.validated_hops.push({ hostname, address_count: 1 });
        queue(machine, { kind: "fetch", url: machine.current, deadline: deadline(machine, at) }, at);         return;     }
    if (hostname === "" || specialName(hostname)) {         failure(machine, "unsafe_address");         return;
    }     queue(machine, { kind: "dns", hostname, deadline: deadline(machine, at) }, at); };
function createFetchMachine(rawUrl, limits, started) {     const initial = rawUrl.length <= limits.max_url_chars ? (0, canonicalize_js_1.canonicalizeUrl)(rawUrl) : { ok: false, reason: "url_too_long" };     const requested = initial.ok ? initial.target.canonical_url : "";
    const machine = { limits, started, current: requested, visited: [], redirects: 0, evidence: { requested_url: requested, final_url: requested, redirect_chain: [], validated_hops: [] }, next_id: 1, pending: null, after_discard: null, terminal: null };     if (!initial.ok)         failure(machine, initial.reason);
    else if (requested.length > limits.max_url_chars)         failure(machine, "url_too_long");     else
        startHop(machine, started);     return machine; }
const discardThen = (machine, action, at) => {     machine.after_discard = action;     queue(machine, { kind: "discard" }, at);
}; const header = (fact, name) => fact.headers[name] ?? { value: null, overflow: false }; function reduceFetchMachine(source, value) {
    const machine = copy(source);     const effect = machine.pending;     if (machine.terminal !== null || effect === null)
        throw new TypeError("invalid machine transition");     const fact = parseFact(value, parseEffect(effect, "pending effect"), "fact");     machine.pending = null;
    if ("deadline" in effect && effect.deadline !== undefined && "completed_at" in fact && fact.completed_at > effect.deadline) {         if (effect.kind === "fetch" || effect.kind === "read")             discardThen(machine, { kind: "fail", reason: "timeout" }, fact.completed_at);
        else             failure(machine, "timeout");         return machine;
    }     if (fact.kind === "dns" && effect.kind === "dns") {         if (fact.failure !== null) {
            failure(machine, fact.failure === "timeout" || fact.failure === "sealed" ? "timeout" : "dns_failure");             return machine;         }
        const addresses = [...new Set(fact.addresses)];         if (fact.overflow || addresses.length === 0 || addresses.length > 32) {             failure(machine, "dns_failure");
            return machine;         }         const publicFlags = addresses.map(address_js_1.isPublicAddress);
        if (!publicFlags.every(Boolean)) {             failure(machine, publicFlags.some(Boolean) ? "mixed_address" : "unsafe_address");             return machine;
        }         machine.evidence.validated_hops.push({ hostname: effect.hostname, address_count: addresses.length });         queue(machine, { kind: "fetch", url: machine.current, deadline: deadline(machine, fact.completed_at) }, fact.completed_at);
        return machine;     }     if (fact.kind === "fetch" && effect.kind === "fetch") {
        if (fact.failure !== null) {             failure(machine, fact.failure === "timeout" || fact.failure === "sealed" ? "timeout" : "fetch_failed");             return machine;
        }         queue(machine, { kind: "metadata", limits: { location: machine.limits.max_url_chars, "content-type": 256, "content-encoding": 128, "content-length": 32 } }, fact.completed_at);         return machine;
    }     if (fact.kind === "metadata" && effect.kind === "metadata") {         if (fact.failure !== null) {
            failure(machine, "invalid_response");             return machine;         }
        const location = header(fact, "location");         const type = header(fact, "content-type");         const encoding = header(fact, "content-encoding");
        const length = header(fact, "content-length");         if (location.overflow) {             discardThen(machine, { kind: "fail", reason: "url_too_long" }, fact.completed_at);
            return machine;         }         if (type.overflow || encoding.overflow || length.overflow) {
            discardThen(machine, { kind: "fail", reason: "invalid_response" }, fact.completed_at);             return machine;         }
        if (fact.status >= 300 && fact.status < 400) {             if (location.value === null) {                 discardThen(machine, { kind: "fail", reason: "redirect_missing_location" }, fact.completed_at);
                return machine;             }             if (machine.redirects >= machine.limits.max_redirects) {
                discardThen(machine, { kind: "fail", reason: "redirect_limit" }, fact.completed_at);                 return machine;             }
            const next = (0, canonicalize_js_1.canonicalizeUrl)(location.value, machine.current);             if (!next.ok || next.target.canonical_url.length > machine.limits.max_url_chars) {                 discardThen(machine, { kind: "fail", reason: next.ok ? "url_too_long" : next.reason }, fact.completed_at);
                return machine;             }             discardThen(machine, { kind: "redirect", url: next.target.canonical_url }, fact.completed_at);
            return machine;         }         if (fact.status < 200 || fact.status >= 300) {
            discardThen(machine, { kind: "fail", reason: "invalid_response" }, fact.completed_at);             return machine;         }
        const normalizedEncoding = encoding.value?.trim().toLowerCase();         if (normalizedEncoding !== undefined && normalizedEncoding !== "" && normalizedEncoding !== "identity") {             discardThen(machine, { kind: "fail", reason: "unsupported_content_encoding" }, fact.completed_at);
            return machine;         }         if (type.value?.split(";", 1)[0]?.trim().toLowerCase() !== "text/html") {
            discardThen(machine, { kind: "fail", reason: "unsupported_content_type" }, fact.completed_at);             return machine;         }
        let declared = null;         if (length.value !== null) {             declared = Number(length.value);
            if (!Number.isSafeInteger(declared) || declared < 0) {                 discardThen(machine, { kind: "fail", reason: "invalid_response" }, fact.completed_at);                 return machine;
            }             if (declared > machine.limits.max_response_bytes) {                 discardThen(machine, { kind: "fail", reason: "response_too_large" }, fact.completed_at);
                return machine;             }         }
        const nextDeadline = deadline(machine, fact.completed_at);         queue(machine, { kind: "read", maximum: machine.limits.max_response_bytes, declared_length: declared, deadline: nextDeadline, token: `${effect.id}:${machine.current}` }, fact.completed_at);         return machine;
    }     if (fact.kind === "discard" && effect.kind === "discard") {         const action = machine.after_discard;
        machine.after_discard = null;         if (action === null)             throw new TypeError("discard without disposition");
        if (action.kind === "fail")             failure(machine, action.reason);         else {
            machine.redirects += 1;             machine.current = action.url;             machine.evidence.redirect_chain.push(action.url);
            machine.evidence.final_url = action.url;             startHop(machine, fact.completed_at);         }
        return machine;     }     if (fact.kind === "read" && effect.kind === "read") {
        if (fact.failure !== null) {             failure(machine, fact.failure === "timeout" ? "timeout" : fact.failure === "limit" ? "response_too_large" : "invalid_response");             return machine;
        }         if (!fact.valid_utf8 || fact.token !== effect.token || fact.length > effect.maximum || (effect.declared_length !== null && fact.length !== effect.declared_length)) {             failure(machine, "invalid_response");
            return machine;         }         machine.terminal = { ok: true, token: fact.token, length: fact.length, digest: fact.digest, evidence: copiedEvidence(machine.evidence) };
        return machine;     }     throw new TypeError("invalid machine fact");
} const freeze = (value) => { if (typeof value === "object" && value !== null) {     for (const child of Object.values(value))
        freeze(child);     Object.freeze(value); } return value; };
const journalEntry = (effect, fact) => freeze(structuredClone({ effect, fact })); exports.journalEntry = journalEntry; function replayFetchMachine(rawUrl, limits, started, entries) {
    let machine = createFetchMachine(rawUrl, limits, started);     for (const raw of entries) {         const entry = record(raw, "journal entry");
        keys(entry, ["effect", "fact"], "journal entry");         if (machine.pending === null || JSON.stringify(parseEffect(machine.pending, "pending effect")) !== JSON.stringify(parseEffect(entry.effect, "journal effect")))             throw new TypeError("journal effect mismatch");
        machine = reduceFetchMachine(machine, entry.fact);     }     if (machine.terminal === null || machine.pending !== null)
        throw new TypeError("journal incomplete");     return machine.terminal; }
}
];
const __cache=[];const __load=(id)=>{if(__cache[id])return __cache[id];const exports={};__cache[id]=exports;__factories[id](exports,__load);return exports;};
const __entry=__load(2);
export const createFetchMachine=__entry.createFetchMachine;
export const journalEntry=__entry.journalEntry;
export const reduceFetchMachine=__entry.reduceFetchMachine;
export const replayFetchMachine=__entry.replayFetchMachine;
