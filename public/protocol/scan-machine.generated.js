// Generated by scripts/sync-protocol.mjs. DO NOT EDIT.
// chunk=scan-machine; target=ES2022; typescript=7.0.2; source-tree-sha256=f47a6e1fb86fc9ed881f48f5392f49b777e3798c21137a523331c48d3446cbd7
import * as __fetch from "./fetch-machine.generated.js";
const __factories=[
(exports,__load)=>{
exports.aggregateAnalysis = aggregateAnalysis; const contracts_js_1 = __load(3); exports.ANALYSIS_LIMITS = { max_provider_observations: 16 };
const RECOGNIZED_MALICIOUS_CATEGORIES = new Set([     "malware",     "social_engineering",
    "unwanted_software",     "potentially_harmful_application", ]);
function timestamp(value, field) {     const parsed = Date.parse(value);     if (!Number.isFinite(parsed))
        throw new TypeError(`${field} must be a timestamp`);     return parsed; }
function freshnessAt(observedAt, expiresAt, evaluatedAt) {     timestamp(observedAt, "observed_at");     const evaluated = timestamp(evaluatedAt, "evaluated_at");
    if (expiresAt === null)         return "unknown";     return timestamp(expiresAt, "expires_at") <= evaluated ? "stale" : "fresh";
} const CONFIDENCE_LIMITATION = "Confidence describes evidence completeness, independence, freshness, and agreement, not likelihood of harm."; function malformedObservation(target, analyzedAt, source) {
    return {         provider: "google_safe_browsing",         source,
        queried_target: target,         observed_at: analyzedAt,         expires_at: null,
        freshness: "unknown",         state: "error",         category: null,
        confidence: "low",         reference: null,         error: "malformed_response",
    }; } function observationSource(value) {
    if (typeof value !== "object" || value === null || Array.isArray(value))         return "fixture";     return value.source === "live" ? "live" : "fixture";
} function normalizedObservation(value, target, analyzedAt) {     const source = observationSource(value);
    let observation;     try {         observation = (0, contracts_js_1.parseProviderObservation)(value);
        const observed = timestamp(observation.observed_at, "observed_at");         const analyzed = timestamp(analyzedAt, "analyzed_at");         if (observation.queried_target !== target || observed > analyzed) {
            return malformedObservation(target, analyzedAt, source);         }         if (observation.expires_at !== null) {
            const expires = timestamp(observation.expires_at, "expires_at");             if (expires < observed)                 return malformedObservation(target, analyzedAt, source);
        }     }     catch {
        return malformedObservation(target, analyzedAt, source);     }     const matchValid = observation.state !== "match" || (observation.error === null &&
        observation.category !== null &&         RECOGNIZED_MALICIOUS_CATEGORIES.has(observation.category));     const noMatchValid = observation.state !== "no_match" || (observation.error === null && observation.category === null);
    const errorValid = observation.state !== "error" || (observation.error !== null && observation.error !== "not_configured" &&         observation.category === null);     const notConfiguredValid = observation.state !== "not_configured" || (observation.error === "not_configured" && observation.category === null);
    if (!matchValid || !noMatchValid || !errorValid || !notConfiguredValid) {         return malformedObservation(target, analyzedAt, source);     }
    const freshness = observation.state === "match" || observation.state === "no_match"         ? freshnessAt(observation.observed_at, observation.expires_at, analyzedAt)         : "unknown";
    return {         ...observation,         freshness,
        confidence: freshness === "fresh" ? "medium" : "low",     }; }
function compareKey(left, right) {     return left < right ? -1 : left > right ? 1 : 0; }
function observationKey(observation) {     return [         observation.provider, observation.source, observation.queried_target, observation.observed_at,
        observation.expires_at ?? "", observation.freshness, observation.state,         observation.category ?? "", observation.confidence,         observation.reference ?? "", observation.error ?? "",
    ].join("\u0000"); } function normalizeObservations(values, target, analyzedAt) {
    if (values.length > exports.ANALYSIS_LIMITS.max_provider_observations) {         throw new RangeError("too many provider observations");     }
    const unique = new Map();     for (const value of values) {         const observation = normalizedObservation(value, target, analyzedAt);
        unique.set(observationKey(observation), observation);     }     return [...unique.values()].sort((left, right) => compareKey(observationKey(left), observationKey(right)));
} function providerEvidence(observation) {     return {
        source: `${observation.source}:${observation.provider}`,         target: observation.queried_target,         category: observation.state === "match"
            ? (observation.category ?? "unrecognized_match")             : "no_known_match",         observed_at: observation.observed_at,
        freshness: observation.freshness,         reference: observation.reference,     };
} function candidateEvidence(target) {     const evidence = target.occurrences.flatMap((occurrence) => {
        if (occurrence.misleading_text === null)             return [];         return [{
                source: `candidate:${occurrence.candidate.provenance.source}`,                 target: target.canonical_url,                 category: "misleading_url_like_text",
                observed_at: occurrence.candidate.provenance.extracted_at,                 freshness: "fresh",                 reference: occurrence.candidate.provenance.document_url,
            }];     });     const unique = new Map(evidence.map((item) => [evidenceKey(item), item]));
    return [...unique.values()].sort((left, right) => compareKey(evidenceKey(left), evidenceKey(right))); } function evidenceKey(evidence) {
    return [evidence.source, evidence.target, evidence.category, evidence.observed_at,         evidence.freshness, evidence.reference ?? ""].join("\u0000"); }
function buildResult(input) {     timestamp(input.analyzed_at, "analyzed_at");     if (input.target === null) {
        return {             scan_id: input.scan_id,             mode: input.mode,
            canonical_target: null,             risk_label: "unknown",             analysis_state: "unscannable",
            confidence: "low",             supporting_evidence: [],             contradicting_evidence: [],
            provider_observations: [],             limitations: [                 `The target could not be scanned (${input.unscannable_reason}).`,
                CONFIDENCE_LIMITATION,             ],         };
    }     const observations = normalizeObservations(input.provider_observations ?? [], input.target.canonical_url, input.analyzed_at);     const candidate = candidateEvidence(input.target);
    const currentMatches = observations.filter((item) => item.state === "match" && item.freshness === "fresh");     const currentNoMatches = observations.filter((item) => item.state === "no_match" && item.freshness === "fresh");     const staleResults = observations.filter((item) => (item.state === "match" || item.state === "no_match") && item.freshness !== "fresh");
    const errors = observations.filter((item) => item.state === "error" || item.state === "not_configured");     let riskLabel = "unknown";     if (currentMatches.length > 0)
        riskLabel = "known_malicious";     else if (candidate.length > 0)         riskLabel = "suspicious";
    else if (currentNoMatches.length > 0)         riskLabel = "no_known_match";     let analysisState;
    if (currentMatches.length > 0 && currentNoMatches.length > 0)         analysisState = "conflicting";     else if (currentMatches.length > 0 || currentNoMatches.length > 0)
        analysisState = "complete";     else if (staleResults.length > 0)         analysisState = "stale";
    else if (errors.length > 0)         analysisState = "provider_error";     else
        analysisState = "unknown";     let confidence = "low";     const hasContradiction = riskLabel === "no_known_match"
        ? observations.some((item) => item.state === "match")         : observations.some((item) => item.state === "no_match");     if (analysisState !== "conflicting" && errors.length === 0 && !hasContradiction) {
        if (riskLabel === "known_malicious")             confidence = candidate.length > 0 ? "high" : "medium";         else if (riskLabel === "no_known_match")
            confidence = "medium";     }     const matchEvidence = observations
        .filter((item) => item.state === "match")         .map(providerEvidence);     const noMatchEvidence = observations
        .filter((item) => item.state === "no_match")         .map(providerEvidence);     const supporting = riskLabel === "no_known_match"
        ? noMatchEvidence         : [...candidate, ...matchEvidence];     const contradicting = riskLabel === "no_known_match"
        ? matchEvidence         : noMatchEvidence;     supporting.sort((left, right) => compareKey(evidenceKey(left), evidenceKey(right)));
    contradicting.sort((left, right) => compareKey(evidenceKey(left), evidenceKey(right)));     const limitations = [];     if (candidate.length > 0) {
        limitations.push("URL-like displayed text resolves to a different target than the link destination.");     }     if (currentNoMatches.length > 0) {
        limitations.push("A current no-match only means the provider returned no known match; it does not assess all risks.");     }     if (currentMatches.length > 0) {
        limitations.push("A recognized provider match is qualified evidence for the reported category, not a complete risk inventory.");     }     if (staleResults.length > 0) {
        limitations.push("Expired or freshness-unknown observations are shown but are not treated as current evidence.");     }     if (errors.length > 0)
        limitations.push("At least one provider lookup did not return usable evidence.");     if (observations.length === 0)         limitations.push("No provider observation was available.");
    if (analysisState === "conflicting") {         limitations.push("Current provider observations materially disagree; supporting and contradicting evidence are both shown.");     }
    limitations.push(CONFIDENCE_LIMITATION);     return {         scan_id: input.scan_id,
        mode: input.mode,         canonical_target: input.target.canonical_url,         risk_label: riskLabel,
        analysis_state: analysisState,         confidence,         supporting_evidence: supporting,
        contradicting_evidence: contradicting,         provider_observations: observations,         limitations,
    }; } function aggregateAnalysis(input) {
    return (0, contracts_js_1.parseScanResult)(buildResult(input)); }
},
(exports,__load)=>{
exports.collectLinkCandidates = collectLinkCandidates; const canonicalize_js_1 = __load(2); const EXPLICIT_URL_TEXT = /^https?:\/\/\S+$/i;
const BARE_URL_TEXT = /^(?:(?:[\p{L}\p{N}](?:[\p{L}\p{N}-]*[\p{L}\p{N}])?\.)+(?:[\p{L}\p{N}](?:[\p{L}\p{N}-]*[\p{L}\p{N}])?))(?::\d+)?(?:[/?#]\S*)?$/iu; function misleadingText(anchorText, linkedTarget) {     const displayedText = anchorText.trim();
    if (!EXPLICIT_URL_TEXT.test(displayedText) && !BARE_URL_TEXT.test(displayedText))         return null;     const linkedScheme = new URL(linkedTarget).protocol;
    const parseableText = EXPLICIT_URL_TEXT.test(displayedText)         ? displayedText         : `${linkedScheme}//${displayedText}`;
    const displayed = (0, canonicalize_js_1.canonicalizeUrl)(parseableText);     if (!displayed.ok || displayed.target.canonical_url === linkedTarget)         return null;
    return {         displayed_text: displayedText,         displayed_target: displayed.target.canonical_url,
        linked_target: linkedTarget,     }; }
function collectLinkCandidates(candidates) {     const targets = new Map();     const rejected = [];
    for (const candidate of candidates) {         const result = (0, canonicalize_js_1.canonicalizeUrl)(candidate.raw_href, candidate.base_url);         if (!result.ok) {
            rejected.push({ candidate, reason: result.reason });             continue;         }
        const key = result.target.canonical_url;         let target = targets.get(key);         if (target === undefined) {
            target = {                 ...result.target,                 occurrences: [],
                anchor_text_variants: [],             };             targets.set(key, target);
        }         const text = candidate.anchor_text.trim();         if (text !== "" && !target.anchor_text_variants.includes(text)) {
            target.anchor_text_variants.push(text);         }         target.occurrences.push({
            candidate,             misleading_text: misleadingText(candidate.anchor_text, key),         });
    }     return { targets: [...targets.values()], rejected }; }
},
(exports,__load)=>{
exports.canonicalizeUrl = canonicalizeUrl; exports.CANONICAL_REJECTION_REASONS = [     "empty_input",
    "missing_base_url",     "invalid_url",     "unsupported_scheme",
    "credentials_not_allowed",     "disallowed_port", ];
exports.FETCH_REJECTION_REASONS = [     "url_too_long", "unsafe_address", "dns_failure", "mixed_address",     "redirect_missing_location", "redirect_loop", "redirect_limit", "timeout",
    "response_too_large", "unsupported_content_type", "unsupported_content_encoding",     "fetch_failed", "invalid_response", "input_too_large", "no_candidates", ];
const ABSOLUTE_SCHEME = /^[A-Za-z][A-Za-z\d+.-]*:/; function canonicalizeUrl(rawHref, baseUrl) {     if (rawHref.trim() === "")
        return { ok: false, reason: "empty_input" };     let parsed;     try {
        try {             parsed = new URL(rawHref);         }
        catch {             if (ABSOLUTE_SCHEME.test(rawHref.trimStart())) {                 return { ok: false, reason: "invalid_url" };
            }             if (baseUrl === undefined || baseUrl.trim() === "") {                 return { ok: false, reason: "missing_base_url" };
            }             const base = new URL(baseUrl);             if (base.protocol !== "http:" && base.protocol !== "https:") {
                return { ok: false, reason: "invalid_url" };             }             parsed = new URL(rawHref, base);
        }     }     catch {
        return { ok: false, reason: "invalid_url" };     }     if (parsed.protocol !== "http:" && parsed.protocol !== "https:") {
        return { ok: false, reason: "unsupported_scheme" };     }     if (parsed.username !== "" || parsed.password !== "") {
        return { ok: false, reason: "credentials_not_allowed" };     }     if (parsed.port !== "")
        return { ok: false, reason: "disallowed_port" };     parsed.hash = "";     const scheme = parsed.protocol === "https:" ? "https" : "http";
    const hostnameAscii = parsed.hostname.toLowerCase();     return {         ok: true,
        target: {             canonical_url: parsed.href,             scheme,
            hostname_ascii: hostnameAscii,             display_hostname: hostnameAscii,         },
    }; }
},
(exports,__load)=>{
exports.parseExtractedLinkCandidate = parseExtractedLinkCandidate; exports.parseProviderObservation = parseProviderObservation; exports.parseScanResult = parseScanResult;
exports.RISK_LABELS = [     "known_malicious",     "suspicious",
    "no_known_match",     "unknown", ];
exports.ANALYSIS_STATES = [     "complete",     "unknown",
    "unscannable",     "provider_error",     "stale",
    "conflicting", ]; exports.CONFIDENCE_LEVELS = ["high", "medium", "low"];
exports.SCAN_MODES = ["paste_url", "paste_html", "live_page"]; exports.FRESHNESS_STATES = ["fresh", "stale", "unknown"]; exports.PROVIDER_IDS = ["google_safe_browsing"];
exports.PROVIDER_SOURCES = ["live", "fixture"]; exports.PROVIDER_STATES = [     "match",
    "no_match",     "error",     "not_configured",
]; exports.PROVIDER_ERROR_CODES = [     "timeout",
    "quota",     "unavailable",     "malformed_response",
    "not_configured", ]; function record(value, path) {
    if (typeof value !== "object" || value === null || Array.isArray(value)) {         throw new TypeError(`${path} must be an object`);     }
    return value; } function exactKeys(value, keys, path) {
    const actual = Object.keys(value).sort();     const expected = [...keys].sort();     if (actual.length !== expected.length || actual.some((key, i) => key !== expected[i])) {
        throw new TypeError(`${path} has unknown or missing fields`);     } }
function literal(value, allowed, path) {     if (typeof value !== "string" || !allowed.includes(value)) {         throw new TypeError(`${path} has an invalid literal`);
    }     return value; }
function string(value, path) {     if (typeof value !== "string")         throw new TypeError(`${path} must be a string`);
    return value; } function nullableString(value, path) {
    return value === null ? null : string(value, path); } function integer(value, path) {
    if (!Number.isSafeInteger(value) || value < 0) {         throw new TypeError(`${path} must be a non-negative integer`);     }
    return value; } function array(value, path, parse) {
    if (!Array.isArray(value))         throw new TypeError(`${path} must be an array`);     return value.map((item, index) => parse(item, `${path}[${index}]`));
} function parseProvenance(value, path) {     const data = record(value, path);
    exactKeys(data, ["source", "document_url", "occurrence_index", "extracted_at"], path);     return {         source: literal(data.source, exports.SCAN_MODES, `${path}.source`),
        document_url: string(data.document_url, `${path}.document_url`),         occurrence_index: integer(data.occurrence_index, `${path}.occurrence_index`),         extracted_at: string(data.extracted_at, `${path}.extracted_at`),
    }; } function parseExtractedLinkCandidate(value) {
    const data = record(value, "candidate");     exactKeys(data, ["raw_href", "anchor_text", "base_url", "provenance"], "candidate");     return {
        raw_href: string(data.raw_href, "candidate.raw_href"),         anchor_text: string(data.anchor_text, "candidate.anchor_text"),         base_url: string(data.base_url, "candidate.base_url"),
        provenance: parseProvenance(data.provenance, "candidate.provenance"),     }; }
function parseProviderObservation(value) {     const path = "provider_observation";     const data = record(value, path);
    exactKeys(data, [         "provider", "source", "queried_target", "observed_at", "expires_at", "freshness",         "state", "category", "confidence", "reference", "error",
    ], path);     return {         provider: literal(data.provider, exports.PROVIDER_IDS, `${path}.provider`),
        source: literal(data.source, exports.PROVIDER_SOURCES, `${path}.source`),         queried_target: string(data.queried_target, `${path}.queried_target`),         observed_at: string(data.observed_at, `${path}.observed_at`),
        expires_at: nullableString(data.expires_at, `${path}.expires_at`),         freshness: literal(data.freshness, exports.FRESHNESS_STATES, `${path}.freshness`),         state: literal(data.state, exports.PROVIDER_STATES, `${path}.state`),
        category: nullableString(data.category, `${path}.category`),         confidence: literal(data.confidence, exports.CONFIDENCE_LEVELS, `${path}.confidence`),         reference: nullableString(data.reference, `${path}.reference`),
        error: data.error === null             ? null             : literal(data.error, exports.PROVIDER_ERROR_CODES, `${path}.error`),
    }; } function parseEvidence(value, path) {
    const data = record(value, path);     exactKeys(data, ["source", "target", "category", "observed_at", "freshness", "reference"], path);     return {
        source: string(data.source, `${path}.source`),         target: string(data.target, `${path}.target`),         category: string(data.category, `${path}.category`),
        observed_at: string(data.observed_at, `${path}.observed_at`),         freshness: literal(data.freshness, exports.FRESHNESS_STATES, `${path}.freshness`),         reference: nullableString(data.reference, `${path}.reference`),
    }; } function parseScanResult(value) {
    const path = "scan_result";     const data = record(value, path);     exactKeys(data, [
        "scan_id", "mode", "canonical_target", "risk_label", "analysis_state",         "confidence", "supporting_evidence", "contradicting_evidence",         "provider_observations", "limitations",
    ], path);     return {         scan_id: string(data.scan_id, `${path}.scan_id`),
        mode: literal(data.mode, exports.SCAN_MODES, `${path}.mode`),         canonical_target: nullableString(data.canonical_target, `${path}.canonical_target`),         risk_label: literal(data.risk_label, exports.RISK_LABELS, `${path}.risk_label`),
        analysis_state: literal(data.analysis_state, exports.ANALYSIS_STATES, `${path}.analysis_state`),         confidence: literal(data.confidence, exports.CONFIDENCE_LEVELS, `${path}.confidence`),         supporting_evidence: array(data.supporting_evidence, `${path}.supporting_evidence`, parseEvidence),
        contradicting_evidence: array(data.contradicting_evidence, `${path}.contradicting_evidence`, parseEvidence),         provider_observations: array(data.provider_observations, `${path}.provider_observations`, parseProviderObservation),         limitations: array(data.limitations, `${path}.limitations`, string),
    }; }
},
(exports,__load)=>{
exports.createScanMachine = createScanMachine; exports.reduceScanMachine = reduceScanMachine; exports.scanJournalEntry = scanJournalEntry;
exports.replayScanMachine = replayScanMachine; exports.verifyScanExchange = verifyScanExchange; const analysis_js_1 = __load(0);
const candidates_js_1 = __load(1); const canonicalize_js_1 = __load(2); const contracts_js_1 = __load(3);
const fetch_machine_js_1 = __fetch; exports.SCAN_LIMITS = { input_chars: 205_000, html_chars: 200_000, occurrences: 256, href_chars: 2_048, anchor_text_chars: 512, results: 16 }; exports.SCAN_LIMITATIONS = {
    anchor_text: "Anchor text was truncated to 512 characters.",     occurrences: "Only the first 256 occurrences were retained; the source contained at least 257.",     results: "Only the first 16 bounded results were retained.",
}; const object = (value, path) => {     if (typeof value !== "object" || value === null || Array.isArray(value))
        throw new TypeError(`${path} must be an object`);     return value; };
const keys = (value, expected, path) => {     const actual = Object.keys(value).sort();     const wanted = [...expected].sort();
    if (actual.length !== wanted.length || actual.some((key, index) => key !== wanted[index]))         throw new TypeError(`${path} has unknown or missing fields`); };
const string = (value, path) => {     if (typeof value !== "string")         throw new TypeError(`${path} must be a string`);
    return value; }; const integer = (value, path, positive = false) => {
    if (!Number.isSafeInteger(value) || value < (positive ? 1 : 0))         throw new TypeError(`${path} must be ${positive ? "a positive" : "a non-negative"} integer`);     return value;
}; const boolean = (value, path) => {     if (typeof value !== "boolean")
        throw new TypeError(`${path} must be a boolean`);     return value; };
const literal = (value, allowed, path) => {     if (typeof value !== "string" || !allowed.includes(value))         throw new TypeError(`${path} has an invalid literal`);
    return value; }; const nullable = (value, path) => (value === null ? null : string(value, path));
const array = (value, path, parser) => {     if (!Array.isArray(value))         throw new TypeError(`${path} must be an array`);
    return value.map((item, index) => parser(item, `${path}[${index}]`)); }; const freeze = (value) => {
    if (typeof value === "object" && value !== null && !Object.isFrozen(value)) {         for (const child of Object.values(value))             freeze(child);
        Object.freeze(value);     }     return value;
}; const frozen = (value) => freeze(structuredClone(value)); const same = (left, right) => {
    if (Object.is(left, right))         return true;     if (typeof left !== "object" || left === null || typeof right !== "object" || right === null || Array.isArray(left) !== Array.isArray(right))
        return false;     if (Array.isArray(left) && Array.isArray(right))         return left.length === right.length && left.every((item, index) => same(item, right[index]));
    const leftRecord = left;     const rightRecord = right;     const names = Object.keys(leftRecord).sort();
    const other = Object.keys(rightRecord).sort();     return names.length === other.length && names.every((name, index) => name === other[index] && same(leftRecord[name], rightRecord[name])); };
const iso = (value, path) => {     const parsed = string(value, path);     let normalized;
    try {         normalized = new Date(parsed).toISOString();     }
    catch {         throw new TypeError(`${path} must be a canonical timestamp`);     }
    if (normalized !== parsed)         throw new TypeError(`${path} must be a canonical timestamp`);     return parsed;
}; function parseLimits(value, path) {     const data = object(value, path);
    keys(data, ["max_url_chars", "max_redirects", "max_response_bytes", "operation_ms", "total_ms"], path);     return { max_url_chars: integer(data.max_url_chars, `${path}.max_url_chars`, true), max_redirects: integer(data.max_redirects, `${path}.max_redirects`, true), max_response_bytes: integer(data.max_response_bytes, `${path}.max_response_bytes`, true), operation_ms: integer(data.operation_ms, `${path}.operation_ms`, true), total_ms: integer(data.total_ms, `${path}.total_ms`, true) }; }
function parseFetchEffect(value, path) {     const data = object(value, path);     const kind = literal(data.kind, ["dns", "fetch", "metadata", "discard", "read"], `${path}.kind`);
    const common = { id: integer(data.id, `${path}.id`, true), issued_at: integer(data.issued_at, `${path}.issued_at`) };     if (kind === "dns") {         keys(data, ["kind", "id", "issued_at", "deadline", "hostname"], path);
        return { kind, hostname: string(data.hostname, `${path}.hostname`), deadline: integer(data.deadline, `${path}.deadline`), ...common };     }     if (kind === "fetch") {
        keys(data, ["kind", "id", "issued_at", "deadline", "url"], path);         return { kind, url: string(data.url, `${path}.url`), deadline: integer(data.deadline, `${path}.deadline`), ...common };     }
    if (kind === "discard") {         keys(data, ["kind", "id", "issued_at"], path);         return { kind, ...common };
    }     if (kind === "metadata") {         keys(data, ["kind", "id", "issued_at", "limits"], path);
        const limits = object(data.limits, `${path}.limits`);         for (const [name, size] of Object.entries(limits))             integer(size, `${path}.limits.${name}`, true);
        return { kind, limits: structuredClone(limits), ...common };     }     keys(data, ["kind", "id", "issued_at", "deadline", "maximum", "declared_length", "token"], path);
    return { kind, maximum: integer(data.maximum, `${path}.maximum`, true), declared_length: data.declared_length === null ? null : integer(data.declared_length, `${path}.declared_length`), deadline: integer(data.deadline, `${path}.deadline`), token: string(data.token, `${path}.token`), ...common }; } function parseFetchFact(value, effect, path) {
    const data = object(value, path);     if (data.kind !== effect.kind)         throw new TypeError(`${path} is not bound to its effect`);
    if (effect.kind === "dns") {         keys(data, ["kind", "completed_at", "addresses", "overflow", "failure"], path);         const addresses = array(data.addresses, `${path}.addresses`, string);
        if (addresses.length > 33)             throw new RangeError(`${path}.addresses is over limit`);         return { kind: "dns", completed_at: integer(data.completed_at, `${path}.completed_at`), addresses, overflow: boolean(data.overflow, `${path}.overflow`), failure: data.failure === null ? null : literal(data.failure, ["timeout", "network_error", "capacity", "sealed", "invalid"], `${path}.failure`) };
    }     if (effect.kind === "fetch") {         keys(data, ["kind", "completed_at", "failure"], path);
        return { kind: "fetch", completed_at: integer(data.completed_at, `${path}.completed_at`), failure: data.failure === null ? null : literal(data.failure, ["timeout", "network_error", "capacity", "sealed", "invalid"], `${path}.failure`) };     }     if (effect.kind === "discard") {
        keys(data, ["kind", "completed_at"], path);         return { kind: "discard", completed_at: integer(data.completed_at, `${path}.completed_at`) };     }
    if (effect.kind === "metadata") {         keys(data, ["kind", "completed_at", "status", "headers", "failure"], path);         const headers = object(data.headers, `${path}.headers`);
        keys(headers, Object.keys(effect.limits), `${path}.headers`);         const parsed = {};         for (const [name, raw] of Object.entries(headers)) {
            const atom = object(raw, `${path}.headers.${name}`);             keys(atom, ["value", "overflow"], `${path}.headers.${name}`);             parsed[name] = { value: nullable(atom.value, `${path}.headers.${name}.value`), overflow: boolean(atom.overflow, `${path}.headers.${name}.overflow`) };
        }         return { kind: "metadata", completed_at: integer(data.completed_at, `${path}.completed_at`), status: integer(data.status, `${path}.status`), headers: parsed, failure: data.failure === null ? null : literal(data.failure, ["invalid"], `${path}.failure`) };     }
    keys(data, ["kind", "completed_at", "failure", "token", "length", "digest", "valid_utf8"], path);     return { kind: "read", completed_at: integer(data.completed_at, `${path}.completed_at`), failure: data.failure === null ? null : literal(data.failure, ["timeout", "limit", "invalid"], `${path}.failure`), token: string(data.token, `${path}.token`), length: integer(data.length, `${path}.length`), digest: string(data.digest, `${path}.digest`), valid_utf8: boolean(data.valid_utf8, `${path}.valid_utf8`) }; }
function parseFetchJournal(value, path) {     return array(value, path, (raw, itemPath) => {         const data = object(raw, itemPath);
        keys(data, ["effect", "fact"], itemPath);         const effect = parseFetchEffect(data.effect, `${itemPath}.effect`);         return { effect, fact: parseFetchFact(data.fact, effect, `${itemPath}.fact`) };
    }); } function parseInput(value) {
    const data = object(value, "input");     const kind = literal(data.kind, ["paste_url", "paste_html", "live_page"], "input.kind");     if (kind === "live_page") {
        keys(data, ["version", "kind", "analyzed_at", "base_url", "document_url"], "input");         if (data.version !== 1)             throw new TypeError("input.version must be 1");
        const base_url = string(data.base_url, "input.base_url"), document_url = string(data.document_url, "input.document_url");         const base = base_url.length <= exports.SCAN_LIMITS.href_chars ? (0, canonicalize_js_1.canonicalizeUrl)(base_url) : { ok: false };         const document = document_url.length <= exports.SCAN_LIMITS.href_chars ? (0, canonicalize_js_1.canonicalizeUrl)(document_url) : { ok: false };
        if (!base.ok || !document.ok || base.target.canonical_url !== document.target.canonical_url)             throw new TypeError("input live URLs are invalid");         return { version: 1, kind, analyzed_at: iso(data.analyzed_at, "input.analyzed_at"), base_url, document_url };
    }     if (kind === "paste_html") {         keys(data, ["version", "kind", "analyzed_at", "base_url", "html"], "input");
        if (data.version !== 1)             throw new TypeError("input.version must be 1");         const html = string(data.html, "input.html");
        if (html.length > exports.SCAN_LIMITS.input_chars)             throw new RangeError("input.html is over limit");         return { version: 1, kind, analyzed_at: iso(data.analyzed_at, "input.analyzed_at"), base_url: string(data.base_url, "input.base_url"), html };
    }     keys(data, ["version", "kind", "analyzed_at", "request_url", "fetch"], "input");     if (data.version !== 1)
        throw new TypeError("input.version must be 1");     const fetch = object(data.fetch, "input.fetch");     keys(fetch, ["started", "limits", "journal"], "input.fetch");
    return { version: 1, kind, analyzed_at: iso(data.analyzed_at, "input.analyzed_at"), request_url: string(data.request_url, "input.request_url"), fetch: { started: integer(fetch.started, "input.fetch.started"), limits: parseLimits(fetch.limits, "input.fetch.limits"), journal: parseFetchJournal(fetch.journal, "input.fetch.journal") } }; } function queued(context, id, effect, phase) {
    return frozen({ version: 1, phase, next_effect_id: id + 1, pending: { ...effect, id }, context, provider_cursor: 0, exchange: null }); } function allocation(machine) {
    const next = structuredClone(machine);     next.phase = "AWAIT_IDS";     next.pending = { kind: "ALLOCATE_IDS", id: next.next_effect_id, count: 1 + next.context.outcomes.length };
    next.next_effect_id += 1;     return frozen(next); }
function sourceContext(input, mode, reason, evidence, base = "") {     return { input, mode, base_url: base, document_url: base, evidence, occurrence_count: { kind: "exact", count: 0 }, groups: [], rejections: [], outcomes: [{ kind: "source", reason }], observations: [], text_overflow: [] }; }
function createScanMachine(value) {     const input = frozen(parseInput(value));     if (input.kind === "live_page") {
        const context = { input, mode: input.kind, base_url: input.base_url, document_url: input.document_url, evidence: null, occurrence_count: { kind: "exact", count: 0 }, groups: [], rejections: [], outcomes: [], observations: [], text_overflow: [] };         return queued(context, 1, { kind: "EXTRACT_HTML", source: input.kind, base_url: input.base_url, document_url: input.document_url, extracted_at: input.analyzed_at, body: { kind: "rendered_anchors" }, limits: { occurrences: 256, href_chars: 2048, anchor_text_chars: 512 } }, "AWAIT_EXTRACT");     }
    let context;     let body;     if (input.kind === "paste_url") {
        const result = (0, fetch_machine_js_1.replayFetchMachine)(input.request_url, input.fetch.limits, input.fetch.started, input.fetch.journal);         if (!result.ok)             return allocation(frozen({ version: 1, phase: "AWAIT_IDS", next_effect_id: 1, pending: null, context: sourceContext(input, input.kind, result.reason, result.evidence), provider_cursor: 0, exchange: null }));
        context = { input, mode: input.kind, base_url: result.evidence.final_url, document_url: result.evidence.final_url, evidence: result.evidence, occurrence_count: { kind: "exact", count: 0 }, groups: [], rejections: [], outcomes: [], observations: [], text_overflow: [] };         body = { kind: "fetch_body", token: result.token, length: result.length, digest: result.digest };     }
    else {         if (input.html.length > exports.SCAN_LIMITS.html_chars)             return allocation(frozen({ version: 1, phase: "AWAIT_IDS", next_effect_id: 1, pending: null, context: sourceContext(input, input.kind, "input_too_large", null), provider_cursor: 0, exchange: null }));
        const base = input.base_url.length <= exports.SCAN_LIMITS.href_chars ? (0, canonicalize_js_1.canonicalizeUrl)(input.base_url) : { ok: false, reason: "url_too_long" };         if (!base.ok || base.target.canonical_url.length > exports.SCAN_LIMITS.href_chars) {             const reason = base.ok ? "url_too_long" : base.reason;
            return allocation(frozen({ version: 1, phase: "AWAIT_IDS", next_effect_id: 1, pending: null, context: sourceContext(input, input.kind, reason, null), provider_cursor: 0, exchange: null }));         }         context = { input, mode: input.kind, base_url: base.target.canonical_url, document_url: base.target.canonical_url, evidence: null, occurrence_count: { kind: "exact", count: 0 }, groups: [], rejections: [], outcomes: [], observations: [], text_overflow: [] };
        body = { kind: "inline_html", html: input.html };     }     return queued(context, 1, { kind: "EXTRACT_HTML", source: input.kind, base_url: context.base_url, document_url: context.document_url, extracted_at: input.analyzed_at, body, limits: { occurrences: 256, href_chars: 2048, anchor_text_chars: 512 } }, "AWAIT_EXTRACT");
} function parseEffect(value, path = "effect") {     const data = object(value, path);
    const kind = literal(data.kind, ["EXTRACT_HTML", "OBSERVE_PROVIDER", "ALLOCATE_IDS"], `${path}.kind`);     const id = integer(data.id, `${path}.id`, true);     if (kind === "OBSERVE_PROVIDER") {
        const batched = Object.hasOwn(data, "batch");         keys(data, ["kind", "id", "target_ordinal", "canonical_target", "requested_at", ...(batched ? ["batch"] : [])], path);         const request = { target_ordinal: integer(data.target_ordinal, `${path}.target_ordinal`), canonical_target: string(data.canonical_target, `${path}.canonical_target`), requested_at: iso(data.requested_at, `${path}.requested_at`) };
        if (!batched)             return { kind, id, ...request };         const batch = array(data.batch, `${path}.batch`, (raw, itemPath) => { const item = object(raw, itemPath); keys(item, ["target_ordinal", "canonical_target", "requested_at"], itemPath); return { target_ordinal: integer(item.target_ordinal, `${itemPath}.target_ordinal`), canonical_target: string(item.canonical_target, `${itemPath}.canonical_target`), requested_at: iso(item.requested_at, `${itemPath}.requested_at`) }; });
        if (batch.length < 1 || batch.length > 16 || !same(batch[0], request))             throw new TypeError(`${path}.batch is invalid`);         return { kind, id, ...request, batch };
    }     if (kind === "ALLOCATE_IDS") {         keys(data, ["kind", "id", "count"], path);
        const count = integer(data.count, `${path}.count`, true);         if (count < 2 || count > 17)             throw new RangeError(`${path}.count is over limit`);
        return { kind, id, count };     }     keys(data, ["kind", "id", "source", "base_url", "document_url", "extracted_at", "body", "limits"], path);
    const limits = object(data.limits, `${path}.limits`);     keys(limits, ["occurrences", "href_chars", "anchor_text_chars"], `${path}.limits`);     if (limits.occurrences !== 256 || limits.href_chars !== 2048 || limits.anchor_text_chars !== 512)
        throw new TypeError(`${path}.limits are invalid`);     const rawBody = object(data.body, `${path}.body`);     const bodyKind = literal(rawBody.kind, ["fetch_body", "inline_html", "rendered_anchors"], `${path}.body.kind`);
    let body;     if (bodyKind === "rendered_anchors") {         keys(rawBody, ["kind"], `${path}.body`);
        body = { kind: bodyKind };     }     else if (bodyKind === "inline_html") {
        keys(rawBody, ["kind", "html"], `${path}.body`);         const html = string(rawBody.html, `${path}.body.html`);         if (html.length > exports.SCAN_LIMITS.html_chars)
            throw new RangeError(`${path}.body.html is over limit`);         body = { kind: bodyKind, html };     }
    else {         keys(rawBody, ["kind", "token", "length", "digest"], `${path}.body`);         body = { kind: bodyKind, token: string(rawBody.token, `${path}.body.token`), length: integer(rawBody.length, `${path}.body.length`), digest: string(rawBody.digest, `${path}.body.digest`) };
    }     const source = literal(data.source, ["paste_url", "paste_html", "live_page"], `${path}.source`);     if ((source === "live_page") !== (body.kind === "rendered_anchors"))
        throw new TypeError(`${path} source/body mismatch`);     return { kind, id, source, base_url: string(data.base_url, `${path}.base_url`), document_url: string(data.document_url, `${path}.document_url`), extracted_at: iso(data.extracted_at, `${path}.extracted_at`), body, limits: { occurrences: 256, href_chars: 2048, anchor_text_chars: 512 } }; }
function parseFact(value, effect, path = "fact") {     const data = object(value, path);     if (effect.kind === "EXTRACT_HTML") {
        keys(data, ["kind", "effect_id", "atoms"], path);         if (data.kind !== "EXTRACTED")             throw new TypeError(`${path} is not bound to its effect`);
        const atoms = array(data.atoms, `${path}.atoms`, (raw, atomPath) => {             const atom = object(raw, atomPath);             if (atom.kind === "OCCURRENCE_OVERFLOW") {
                keys(atom, ["kind"], atomPath);                 return { kind: "OCCURRENCE_OVERFLOW" };             }
            keys(atom, ["kind", "href", "href_overflow", "text", "text_overflow"], atomPath);             if (atom.kind !== "ANCHOR")                 throw new TypeError(`${atomPath}.kind is invalid`);
            const overflow = boolean(atom.href_overflow, `${atomPath}.href_overflow`);             const href = nullable(atom.href, `${atomPath}.href`);             if ((overflow && href !== null) || (!overflow && href === null) || (href !== null && href.length > exports.SCAN_LIMITS.href_chars))
                throw new TypeError(`${atomPath}.href binding is invalid`);             const text = string(atom.text, `${atomPath}.text`);             if (text.length > exports.SCAN_LIMITS.anchor_text_chars)
                throw new RangeError(`${atomPath}.text is over limit`);             return { kind: "ANCHOR", href, href_overflow: overflow, text, text_overflow: boolean(atom.text_overflow, `${atomPath}.text_overflow`) };         });
        const sentinel = atoms.findIndex((atom) => atom.kind === "OCCURRENCE_OVERFLOW");         if (atoms.length > 257 || (sentinel >= 0 && (sentinel !== 256 || atoms.length !== 257)) || (sentinel < 0 && atoms.length > 256))             throw new TypeError(`${path}.atoms has an invalid occurrence boundary`);
        return { kind: "EXTRACTED", effect_id: integer(data.effect_id, `${path}.effect_id`, true), atoms };     }     if (effect.kind === "OBSERVE_PROVIDER") {
        const batched = effect.batch !== undefined;         keys(data, ["kind", "effect_id", "observation", ...(batched ? ["batch"] : [])], path);         if (data.kind !== "PROVIDER_OBSERVED")
            throw new TypeError(`${path} is not bound to its effect`);         const parseObservation = (value, itemPath) => { const raw = object(value, itemPath); keys(raw, ["provider", "source", "queried_target", "observed_at", "expires_at", "state", "category", "reference", "error"], itemPath); return { provider: literal(raw.provider, contracts_js_1.PROVIDER_IDS, `${itemPath}.provider`), source: literal(raw.source, contracts_js_1.PROVIDER_SOURCES, `${itemPath}.source`), queried_target: string(raw.queried_target, `${itemPath}.queried_target`), observed_at: string(raw.observed_at, `${itemPath}.observed_at`), expires_at: nullable(raw.expires_at, `${itemPath}.expires_at`), state: literal(raw.state, contracts_js_1.PROVIDER_STATES, `${itemPath}.state`), category: nullable(raw.category, `${itemPath}.category`), reference: nullable(raw.reference, `${itemPath}.reference`), error: raw.error === null ? null : literal(raw.error, contracts_js_1.PROVIDER_ERROR_CODES, `${itemPath}.error`) }; };         const observation = parseObservation(data.observation, `${path}.observation`);
        if (observation.queried_target !== effect.canonical_target)             throw new TypeError(`${path}.observation target mismatch`);         if (!batched)
            return { kind: "PROVIDER_OBSERVED", effect_id: integer(data.effect_id, `${path}.effect_id`, true), observation };         const batch = array(data.batch, `${path}.batch`, parseObservation);         if (batch.length !== effect.batch.length || !same(batch[0], observation) || batch.some((item, index) => item.queried_target !== effect.batch[index].canonical_target))
            throw new TypeError(`${path}.batch target mismatch`);         return { kind: "PROVIDER_OBSERVED", effect_id: integer(data.effect_id, `${path}.effect_id`, true), observation, batch };     }
    keys(data, ["kind", "effect_id", "ids"], path);     if (data.kind !== "IDS_ALLOCATED")         throw new TypeError(`${path} is not bound to its effect`);
    const ids = array(data.ids, `${path}.ids`, string);     if (ids.length !== effect.count || new Set(ids).size !== ids.length || ids.some((id) => !/^[0-9a-f]{32}$/u.test(id)))         throw new TypeError(`${path}.ids are invalid`);
    return { kind: "IDS_ALLOCATED", effect_id: integer(data.effect_id, `${path}.effect_id`, true), ids }; } function afterExtraction(machine, fact) {
    const next = structuredClone(machine);     const anchors = fact.atoms.filter((atom) => atom.kind === "ANCHOR");     const candidates = anchors.flatMap((atom, index) => (atom.href === null ? [] : [{ raw_href: atom.href, anchor_text: atom.text, base_url: next.context.base_url, provenance: { source: next.context.mode, document_url: next.context.document_url, occurrence_index: index, extracted_at: next.context.input.analyzed_at } }]));
    const collection = (0, candidates_js_1.collectLinkCandidates)(candidates);     const groups = collection.targets.filter((target) => target.canonical_url.length <= exports.SCAN_LIMITS.href_chars);     const rejected = collection.rejected.map(({ candidate, reason }) => ({ occurrence_index: candidate.provenance.occurrence_index, reason }));
    for (const target of collection.targets)         if (target.canonical_url.length > exports.SCAN_LIMITS.href_chars)             for (const occurrence of target.occurrences)
                rejected.push({ occurrence_index: occurrence.candidate.provenance.occurrence_index, reason: "url_too_long" });     anchors.forEach((atom, index) => {         if (atom.href_overflow)
            rejected.push({ occurrence_index: index, reason: "url_too_long" });     });     rejected.sort((a, b) => a.occurrence_index - b.occurrence_index);
    next.context.groups = groups;     next.context.rejections = rejected.map((item, rejection_ordinal) => ({ ...item, rejection_ordinal }));     const outcomes = [...groups.map((_, target_ordinal) => ({ kind: "target", target_ordinal })), ...next.context.rejections.map(({ rejection_ordinal }) => ({ kind: "rejection", rejection_ordinal }))];
    next.context.outcomes = outcomes.length === 0 ? [{ kind: "source", reason: "no_candidates" }] : outcomes.slice(0, exports.SCAN_LIMITS.results);     next.context.occurrence_count = fact.atoms.at(-1)?.kind === "OCCURRENCE_OVERFLOW" ? { kind: "at_least", count: 257 } : { kind: "exact", count: anchors.length };     next.context.text_overflow = anchors.flatMap((atom, index) => (atom.text_overflow ? [index] : []));
    next.provider_cursor = 0;     if (next.context.outcomes[0]?.kind === "target") {         const target = groups[0];
        next.phase = "AWAIT_PROVIDER";         next.pending = { kind: "OBSERVE_PROVIDER", id: next.next_effect_id, target_ordinal: 0, canonical_target: target.canonical_url, requested_at: next.context.input.analyzed_at };         if (next.context.mode === "live_page")
            next.pending.batch = next.context.outcomes.flatMap((outcome) => outcome.kind === "target" ? [{ target_ordinal: outcome.target_ordinal, canonical_target: groups[outcome.target_ordinal].canonical_url, requested_at: next.context.input.analyzed_at }] : []);         next.next_effect_id += 1;         return frozen(next);
    }     return allocation(frozen(next)); }
function resultFor(context, outcome, id) {     if (outcome.kind !== "target")         return (0, analysis_js_1.aggregateAnalysis)({ scan_id: id, mode: context.mode, analyzed_at: context.input.analyzed_at, target: null, unscannable_reason: outcome.kind === "source" ? outcome.reason : context.rejections[outcome.rejection_ordinal].reason });
    const target = context.groups[outcome.target_ordinal];     const primitive = context.observations[outcome.target_ordinal];     return (0, analysis_js_1.aggregateAnalysis)({ scan_id: id, mode: context.mode, analyzed_at: context.input.analyzed_at, target, provider_observations: [{ ...primitive, freshness: "unknown", confidence: "low" }] });
} function terminal(machine, ids) {     const next = structuredClone(machine);
    const sentinel = next.context.occurrence_count.kind === "at_least";     const total = next.context.groups.length + next.context.rejections.length;     const entries = next.context.outcomes.map((outcome, result_ordinal) => {
        const result_id = ids[result_ordinal + 1];         const result = resultFor(next.context, outcome, result_id);         if (outcome.kind === "target" && next.context.groups[outcome.target_ordinal].occurrences.some(({ candidate }) => next.context.text_overflow.includes(candidate.provenance.occurrence_index)))
            result.limitations.push(exports.SCAN_LIMITATIONS.anchor_text);         if (sentinel)             result.limitations.push(exports.SCAN_LIMITATIONS.occurrences);
        if (total > exports.SCAN_LIMITS.results)             result.limitations.push(exports.SCAN_LIMITATIONS.results);         return { result_ordinal, result_id, outcome, result: (0, contracts_js_1.parseScanResult)(result) };
    });     const representedTargets = new Set(next.context.outcomes.flatMap((outcome) => (outcome.kind === "target" ? [outcome.target_ordinal] : [])));     const representedRejections = new Set(next.context.outcomes.flatMap((outcome) => (outcome.kind === "rejection" ? [outcome.rejection_ordinal] : [])));
    const targets = next.context.groups.flatMap((target, target_ordinal) => (next.context.mode === "live_page" ? target_ordinal < exports.SCAN_LIMITS.results : representedTargets.has(target_ordinal)) ? [{ target_ordinal, canonical_url: target.canonical_url, occurrences: target.occurrences.map(({ candidate }) => ({ occurrence_index: candidate.provenance.occurrence_index, raw_href: candidate.raw_href, anchor_text: candidate.anchor_text, anchor_text_overflow: next.context.text_overflow.includes(candidate.provenance.occurrence_index) })), anchor_text_variants: [...target.anchor_text_variants] }] : []);     const rejections = next.context.mode === "live_page" ? next.context.rejections.slice(0, exports.SCAN_LIMITS.results) : next.context.rejections.filter(({ rejection_ordinal }) => representedRejections.has(rejection_ordinal));     const only = next.context.groups.length === 0 && next.context.rejections.length === 1 ? next.context.rejections[0].reason : next.context.outcomes[0]?.kind === "source" ? next.context.outcomes[0].reason : null;
    next.exchange = { version: 1, receipt: { receipt_id: ids[0], mode: next.context.mode, scan_ids: ids.slice(1), occurrence_count: next.context.occurrence_count, accepted_targets: next.context.groups.length, rejected_candidates: next.context.rejections.length, truncated: sentinel || total > exports.SCAN_LIMITS.results, unscannable_reason: only, fetch_evidence: next.context.evidence, targets, rejections }, entries };     next.phase = "DONE";     next.pending = null;
    return frozen(next); } function reduceScanMachine(source, value) {
    if (source.exchange !== null || source.pending === null)         throw new TypeError("invalid machine transition");     const effect = parseEffect(source.pending);
    const fact = parseFact(value, effect);     if (fact.effect_id !== effect.id)         throw new TypeError("fact effect mismatch");
    if (effect.kind === "EXTRACT_HTML" && fact.kind === "EXTRACTED")         return afterExtraction(source, fact);     if (effect.kind === "OBSERVE_PROVIDER" && fact.kind === "PROVIDER_OBSERVED") {
        const next = structuredClone(source);         if (effect.batch !== undefined) {             next.context.observations.push(...fact.batch);
            next.provider_cursor = effect.batch.length;             return allocation(frozen(next));         }
        next.context.observations.push(fact.observation);         next.provider_cursor += 1;         const following = next.context.outcomes[next.provider_cursor];
        if (following?.kind === "target") {             const target = next.context.groups[following.target_ordinal];             next.pending = { kind: "OBSERVE_PROVIDER", id: next.next_effect_id, target_ordinal: following.target_ordinal, canonical_target: target.canonical_url, requested_at: next.context.input.analyzed_at };
            next.next_effect_id += 1;             return frozen(next);         }
        return allocation(frozen(next));     }     if (effect.kind === "ALLOCATE_IDS" && fact.kind === "IDS_ALLOCATED")
        return terminal(source, fact.ids);     throw new TypeError("invalid machine fact"); }
function scanJournalEntry(effectValue, factValue) {     const effect = parseEffect(effectValue);     const fact = parseFact(factValue, effect);
    if (fact.effect_id !== effect.id)         throw new TypeError("fact effect mismatch");     return frozen({ effect, fact });
} function replayScanMachine(input, journal) {     if (!Array.isArray(journal))
        throw new TypeError("journal must be an array");     let machine = createScanMachine(input);     for (const raw of journal) {
        const entryData = object(raw, "journal entry");         keys(entryData, ["effect", "fact"], "journal entry");         const entry = scanJournalEntry(entryData.effect, entryData.fact);
        if (machine.pending === null || !same(machine.pending, entry.effect))             throw new TypeError("journal effect mismatch");         machine = reduceScanMachine(machine, entry.fact);
    }     if (machine.phase !== "DONE" || machine.pending !== null || machine.exchange === null)         throw new TypeError("journal incomplete");
    return frozen(machine.exchange); } function validateClaim(value) {
    const data = object(value, "exchange");     keys(data, ["version", "receipt", "entries"], "exchange");     if (data.version !== 1)
        throw new TypeError("exchange.version must be 1");     const receipt = object(data.receipt, "exchange.receipt");     keys(receipt, ["receipt_id", "mode", "scan_ids", "occurrence_count", "accepted_targets", "rejected_candidates", "truncated", "unscannable_reason", "fetch_evidence", "targets", "rejections"], "exchange.receipt");
    array(receipt.scan_ids, "exchange.receipt.scan_ids", string);     const count = object(receipt.occurrence_count, "exchange.receipt.occurrence_count");     keys(count, ["kind", "count"], "exchange.receipt.occurrence_count");
    array(receipt.targets, "exchange.receipt.targets", (raw, path) => {         const item = object(raw, path);         keys(item, ["target_ordinal", "canonical_url", "occurrences", "anchor_text_variants"], path);
        array(item.occurrences, `${path}.occurrences`, (occ, occPath) => {             const atom = object(occ, occPath);             keys(atom, ["occurrence_index", "raw_href", "anchor_text", "anchor_text_overflow"], occPath);
            return atom;         });         array(item.anchor_text_variants, `${path}.anchor_text_variants`, string);
        return item;     });     array(receipt.rejections, "exchange.receipt.rejections", (raw, path) => {
        const item = object(raw, path);         keys(item, ["rejection_ordinal", "occurrence_index", "reason"], path);         return item;
    });     array(data.entries, "exchange.entries", (raw, path) => {         const item = object(raw, path);
        keys(item, ["result_ordinal", "result_id", "outcome", "result"], path);         const outcome = object(item.outcome, `${path}.outcome`);         if (outcome.kind === "target")
            keys(outcome, ["kind", "target_ordinal"], `${path}.outcome`);         else if (outcome.kind === "rejection")             keys(outcome, ["kind", "rejection_ordinal"], `${path}.outcome`);
        else if (outcome.kind === "source")             keys(outcome, ["kind", "reason"], `${path}.outcome`);         else
            throw new TypeError(`${path}.outcome.kind is invalid`);         (0, contracts_js_1.parseScanResult)(item.result);         return item;
    }); } function verifyScanExchange(input, journal, claimed) {
    validateClaim(claimed);     const derived = replayScanMachine(input, journal);     if (!same(derived, claimed))
        throw new TypeError("scan exchange mismatch");     return derived; }
}
];
const __cache=[];const __load=(id)=>{if(__cache[id])return __cache[id];const exports={};__cache[id]=exports;__factories[id](exports,__load);return exports;};
const __entry=__load(4);
export const SCAN_LIMITATIONS=__entry.SCAN_LIMITATIONS;
export const SCAN_LIMITS=__entry.SCAN_LIMITS;
export const createScanMachine=__entry.createScanMachine;
export const reduceScanMachine=__entry.reduceScanMachine;
export const replayScanMachine=__entry.replayScanMachine;
export const scanJournalEntry=__entry.scanJournalEntry;
export const verifyScanExchange=__entry.verifyScanExchange;
